# Shared project context

Strategos is a dependency-free Node.js CLI. Its first milestone coordinates
Claude Code, Codex CLI, and GitHub Copilot CLI through explicit JSON plans.

All writing tasks run in separate Git worktrees. The orchestrator preserves
reports and branches for human review rather than merging automatically.
